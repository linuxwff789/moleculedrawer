name: Android Build

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
  workflow_dispatch:        # 支持手动触发

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew :moleculedrawer:assembleDebug --no-daemon --stacktrace
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}

      - name: Upload Debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: moleculedrawer-debug-apk
          path: moleculedrawer/build/outputs/apk/debug/*.apk
          if-no-files-found: error
          retention-days: 14

  # 可选：Release 构建（需要签名配置，当前无签名文件时注释掉）
  # release:
  #   name: Build Release APK
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-java@v4
  #       with:
  #         distribution: temurin
  #         java-version: '17'
  #     - uses: android-actions/setup-android@v3
  #     - run: chmod +x gradlew
  #     - run: ./gradlew :moleculedrawer:assembleRelease --no-daemon
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: moleculedrawer-release-apk
  #         path: moleculedrawer/build/outputs/apk/release/*.apk
